services:
  app:
    build: .
    container_name: spbu-dashboard
    ports:
      - "8050:8050"
    environment:
      DASH_DEBUG: "1"
    volumes:
      # Код + БД живут на хосте: pure_data.db не пропадает при пересборке
      - ./app:/app/app
    restart: unless-stopped
